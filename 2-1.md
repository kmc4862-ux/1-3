# 나만의 프롬프트 관리 프로그램

CATEGORIES = [
    "텍스트 생성",
    "이미지 생성",
    "영상 생성",
    "페르소나",
    "자동화",
    "기타"
]

prompts = [
    {
        "title": "블로그 글 작성 도우미",
        "content": "주어진 주제에 대해 SEO에 최적화된 블로그 글을 작성해주세요. 서론, 본론, 결론 구조를 갖추고 제목을 3개 제안해주세요.",
        "category": "텍스트 생성",
        "favorite": True
    },
    {
        "title": "제품 썸네일 생성",
        "content": "제품의 특징과 장점을 강조하는 매력적인 썸네일 이미지를 생성해주세요. 깔끔하고 현대적인 디자인을 사용해주세요.",
        "category": "이미지 생성",
        "favorite": False
    },
    {
        "title": "IT 컨설턴트 페르소나",
        "content": "당신은 10년 이상의 경력을 가진 IT 컨설턴트입니다. 복잡한 기술 내용을 초보자도 이해하기 쉽게 설명해주세요.",
        "category": "페르소나",
        "favorite": False
    },
    {
        "title": "뉴스 요약 자동화",
        "content": "입력된 뉴스 내용을 핵심 내용 중심으로 5문장 이내로 요약하고 주요 키워드 5개를 추출해주세요.",
        "category": "자동화",
        "favorite": False
    }
]


def show_menu():
    print("\n" + "=" * 40)
    print("       나만의 프롬프트 관리")
    print("=" * 40)
    print("1. 프롬프트 추가")
    print("2. 프롬프트 목록")
    print("3. 카테고리별 조회")
    print("4. 프롬프트 검색")
    print("5. 프롬프트 상세 보기")
    print("6. 즐겨찾기 관리")
    print("7. 즐겨찾기 목록")
    print("0. 종료")
    print("=" * 40)


def input_required(message):
    while True:
        value = input(message).strip()

        if value:
            return value

        print("입력값이 비어 있습니다. 다시 입력해주세요.")


def select_category():
    print("\n카테고리 선택")

    for index, category in enumerate(CATEGORIES, 1):
        print(f"{index}) {category}")
    print("7) 직접 입력")

    while True:
        choice = input("선택: ").strip()

        if choice.isdigit():
            number = int(choice)

            if 1 <= number <= len(CATEGORIES):
                return CATEGORIES[number - 1]

            if number == 7:
                return input_required("카테고리 입력: ")

        print("올바른 카테고리를 선택해주세요.")


def add_prompt():
    print("\n=== 프롬프트 추가 ===")

    title = input_required("제목: ")
    content = input_required("내용: ")
    category = select_category()

    prompt = {
        "title": title,
        "content": content,
        "category": category,
        "favorite": False
    }

    prompts.append(prompt)

    print("\n프롬프트가 추가되었습니다!")


def print_prompt_item(index, prompt):
    favorite = " ⭐" if prompt["favorite"] else ""
    print(
        f"{index}. [{prompt['category']}] "
        f"{prompt['title']}{favorite}"
    )


def show_list(prompt_list=None):
    print("\n=== 프롬프트 목록 ===")

    if prompt_list is None:
        prompt_list = prompts

    if not prompt_list:
        print("등록된 프롬프트가 없습니다.")
        return

    for index, prompt in enumerate(prompt_list, 1):
        print_prompt_item(index, prompt)

    print(f"\n총 {len(prompt_list)}개의 프롬프트")


def select_category_for_view():
    print("\n=== 카테고리별 조회 ===")

    for index, category in enumerate(CATEGORIES, 1):
        print(f"{index}) {category}")

    while True:
        choice = input("선택: ").strip()

        if choice.isdigit():
            number = int(choice)

            if 1 <= number <= len(CATEGORIES):
                return CATEGORIES[number - 1]

        print("올바른 번호를 입력해주세요.")


def show_by_category():
    category = select_category_for_view()

    results = [
        prompt for prompt in prompts
        if prompt["category"] == category
    ]

    print(f"\n[{category}] 카테고리 프롬프트")

    if not results:
        print("해당 카테고리에 프롬프트가 없습니다.")
        return

    for index, prompt in enumerate(results, 1):
        print_prompt_item(index, prompt)

    print(f"\n총 {len(results)}개의 프롬프트")


def search_prompt():
    print("\n=== 프롬프트 검색 ===")

    keyword = input_required("검색어: ").lower()

    results = [
        prompt for prompt in prompts
        if keyword in prompt["title"].lower()
        or keyword in prompt["content"].lower()
    ]

    print("\n검색 결과")

    if not results:
        print("검색 결과가 없습니다.")
        return

    for index, prompt in enumerate(results, 1):
        print_prompt_item(index, prompt)

    print(f"\n{len(results)}개의 프롬프트를 찾았습니다.")


def get_prompt_number():
    if not prompts:
        print("등록된 프롬프트가 없습니다.")
        return None

    while True:
        choice = input("번호 입력: ").strip()

        if choice.isdigit():
            number = int(choice)

            if 1 <= number <= len(prompts):
                return number - 1

        print("잘못된 번호입니다. 다시 입력해주세요.")


def show_detail():
    print("\n=== 프롬프트 상세 보기 ===")

    index = get_prompt_number()

    if index is None:
        return

    prompt = prompts[index]

    favorite = "⭐" if prompt["favorite"] else "없음"

    print("\n" + "-" * 40)
    print(f"제목: {prompt['title']}")
    print(f"카테고리: {prompt['category']}")
    print(f"즐겨찾기: {favorite}")
    print("-" * 40)
    print("내용:")
    print(prompt["content"])
    print("-" * 40)


def manage_favorite():
    print("\n=== 즐겨찾기 관리 ===")

    index = get_prompt_number()

    if index is None:
        return

    prompt = prompts[index]

    prompt["favorite"] = not prompt["favorite"]

    if prompt["favorite"]:
        print(
            f"'{prompt['title']}' 프롬프트를 "
            "즐겨찾기에 추가했습니다!"
        )
    else:
        print(
            f"'{prompt['title']}' 프롬프트를 "
            "즐겨찾기에서 해제했습니다!"
        )


def show_favorites():
    print("\n=== 즐겨찾기 목록 ===")

    favorites = [
        prompt for prompt in prompts
        if prompt["favorite"]
    ]

    if not favorites:
        print("즐겨찾기한 프롬프트가 없습니다.")
        return

    for index, prompt in enumerate(favorites, 1):
        print_prompt_item(index, prompt)

    print(f"\n총 {len(favorites)}개의 즐겨찾기")


def main():
    print("\n나만의 프롬프트 관리 프로그램을 시작합니다!")

    while True:
        show_menu()

        choice = input("선택: ").strip()

        if choice == "1":
            add_prompt()

        elif choice == "2":
            show_list()

        elif choice == "3":
            show_by_category()

        elif choice == "4":
            search_prompt()

        elif choice == "5":
            show_detail()

        elif choice == "6":
            manage_favorite()

        elif choice == "7":
            show_favorites()

        elif choice == "0":
            print("\n프로그램을 종료합니다. 감사합니다!")
            break

        else:
            print("\n잘못된 메뉴 번호입니다.")
            print("0~7 사이의 번호를 입력해주세요.")


if __name__ == "__main__":
    main()
나만의 프롬프트 관리 프로그램

Python 기초 문법과 Git/GitHub를 활용하여 제작한 콘솔 기반 프롬프트 관리 프로그램입니다.

흩어져 있는 프롬프트를 하나의 프로그램에서 관리할 수 있도록 프롬프트 추가, 목록 조회, 카테고리별 조회, 검색, 상세 보기, 즐겨찾기 기능을 구현했습니다.

개발 환경

Python 3.10 이상

Visual Studio Code

Git

GitHub

실행 방법

프로젝트 폴더에서 다음 명령어를 실행합니다.

python prompt_manager.py


프로그램이 실행되면 메뉴에서 원하는 기능의 번호를 입력합니다.

주요 기능
1. 프롬프트 추가

제목, 내용, 카테고리를 입력하여 새로운 프롬프트를 등록할 수 있습니다.

2. 프롬프트 목록

등록된 모든 프롬프트를 번호와 함께 확인할 수 있습니다.

즐겨찾기로 등록된 프롬프트에는 ⭐ 표시가 나타납니다.

3. 카테고리별 조회

다음 카테고리를 기준으로 프롬프트를 조회할 수 있습니다.

텍스트 생성

이미지 생성

영상 생성

페르소나

자동화

기타

4. 프롬프트 검색

검색어가 프롬프트 제목 또는 내용에 포함되어 있는 경우 해당 프롬프트를 검색할 수 있습니다.

5. 프롬프트 상세 보기

선택한 프롬프트의 제목, 카테고리, 즐겨찾기 여부와 전체 내용을 확인할 수 있습니다.

6. 즐겨찾기 관리

프롬프트를 즐겨찾기에 추가하거나 해제할 수 있습니다.

7. 즐겨찾기 목록

즐겨찾기로 등록한 프롬프트만 모아서 확인할 수 있습니다.

데이터 구조

프롬프트는 리스트와 딕셔너리를 이용하여 관리합니다.

prompts = [
    {
        "title": "블로그 글 작성 도우미",
        "content": "프롬프트 내용",
        "category": "텍스트 생성",
        "favorite": True
    }
]


프로그램 실행 중 추가하거나 변경한 데이터는 프로그램이 종료되면 초기화됩니다.

프로젝트 목표

이 프로젝트를 통해 다음 내용을 실습했습니다.

Python 변수와 자료형

리스트와 딕셔너리

조건문

반복문

함수

사용자 입력 처리

Git 저장소 생성 및 관리

Git commit

Git branch

Git merge

GitHub 원격 저장소 사용

Git 작업

기능을 작은 단위로 나누어 커밋하고, 별도의 브랜치에서 기능을 개발한 후 main 브랜치에 병합하는 방식으로 프로젝트를 관리했습니다.

프로젝트 구조
prompt-manager/
├── prompt_manager.py
├── README.md
└── .gitignore
