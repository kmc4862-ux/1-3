1. 서비스 기획서
서비스명
TripMate — AI 여행 플래너

서비스 목적
여행을 계획할 때 장소를 하나씩 검색해야 하는 불편함을 줄이고, 사용자가 입력한 여행 정보를 바탕으로 AI가 간단한 여행 일정을 추천해주는 웹 서비스이다.

타겟 사용자
여행 계획을 세우는 것이 어려운 사람

짧은 시간에 여행 일정을 만들고 싶은 사람

여행지와 취향에 맞는 코스를 추천받고 싶은 사람

핵심 기능
사용자가 여행지, 여행 기간, 여행 스타일을 입력하면 AI가 날짜별 여행 일정을 만들어준다.

예시:

여행지: 부산
기간: 2박 3일
스타일: 맛집과 바다를 좋아함

→ AI가 1일차부터 3일차까지 방문 장소와 간단한 설명을 추천한다.

2. 페이지 구성
웹사이트는 메뉴를 통해 총 3개의 주요 섹션으로 이동할 수 있도록 구성한다.

① 홈
서비스 이름과 간단한 소개

"AI 여행 일정 만들기" 버튼

서비스의 핵심 기능 소개

깔끔한 여행 관련 이미지와 디자인

② AI 여행 추천
사용자가 여행 정보를 입력하는 핵심 페이지이다.

입력 항목:

여행지

여행 기간

여행 스타일

추가 요청사항

예시:

여행지: 제주도
여행 기간: 3일
여행 스타일: 자연, 맛집
추가 요청: 너무 빡빡하지 않은 일정

입력 후 "일정 추천받기" 버튼을 누르면 AI가 결과를 생성한다.

③ 서비스 소개
TripMate가 어떤 서비스인지 설명

AI 추천 기능의 작동 방식

사용자가 서비스를 이용하는 방법

간단한 FAQ

3. AI 기능 설계
입력
사용자가 다음 정보를 입력한다.

여행지
여행 기간
여행 스타일
추가 요청사항

처리
프론트엔드 JavaScript가 사용자의 입력을 fetch()로 Python API에 전달한다.

Python Serverless Function에서 AI API를 호출하고 결과를 다시 프론트엔드로 전달한다.

출력
AI가 다음과 같은 형식으로 여행 일정을 생성한다.

[1일차]
09:00 - 성산일출봉 방문
12:00 - 근처 맛집에서 점심
14:00 - 섭지코지 산책
18:00 - 저녁 식사

[2일차]
10:00 - 한라산 주변 관광
13:00 - 제주 전통 음식점
15:00 - 카페 방문
...

사용자는 생성된 결과를 웹페이지에서 바로 확인할 수 있다.

4. 실패 처리
빈 입력
여행지나 기간을 입력하지 않고 버튼을 누르면:

"여행지와 여행 기간을 입력해주세요."

라는 메시지를 보여준다.

API 오류
AI API에 문제가 발생하면:

"일정을 생성하지 못했습니다. 잠시 후 다시 시도해주세요."

라고 안내한다.

응답 지연
AI 응답을 기다리는 동안:

"AI가 여행 일정을 만드는 중입니다..."

라는 로딩 메시지를 표시한다.

5. 프로젝트 구조
tripmate/
│
├── index.html
│
├── css/
│   └── style.css
│
├── js/
│   └── script.js
│
├── api/
│   └── recommend.py
│
├── images/
│   └── travel.jpg
│
├── requirements.txt
│
└── README.md

각 파일의 역할은 다음과 같다.

index.html → 웹페이지 구조

style.css → 색상, 레이아웃, 반응형 디자인

script.js → 버튼 클릭, 입력값 처리, API 요청

api/recommend.py → AI API 호출을 담당하는 백엔드

requirements.txt → Python 패키지 관리

README.md → 프로젝트 설명 및 실행 방법

6. AI 동작 과정
① 사용자가 여행 정보 입력
        ↓
② "일정 추천받기" 클릭
        ↓
③ JavaScript에서 입력값 확인
        ↓
④ fetch('/api/recommend') 실행
        ↓
⑤ Python Serverless Function 실행
        ↓
⑥ AI API에 여행 정보 전달
        ↓
⑦ AI가 여행 일정 생성
        ↓
⑧ Python이 결과 반환
        ↓
⑨ JavaScript가 결과를 화면에 표시

이 과정을 통해 HTML → JavaScript → Python API → AI → JavaScript → 화면으로 이어지는 구조를 설명할 수 있다.

7. 기술 스택
Frontend: HTML / CSS / JavaScript

Backend: Python

Serverless: Vercel Serverless Functions

AI: OpenAI API

배포: Vercel

코드 관리: GitHub

React나 Vue 같은 프레임워크는 사용하지 않는다.

8. 환경 변수
AI API 키는 코드에 직접 작성하지 않고 Vercel 환경 변수에 저장한다.

예:

OPENAI_API_KEY=발급받은_API_키

API 키가 GitHub에 올라가면 다른 사람이 사용할 수 있기 때문에 소스 코드나 README에 실제 키를 작성하지 않는다.

9. README.md 구성
README에는 다음 내용을 작성한다.

# TripMate

AI를 활용한 맞춤 여행 일정 추천 서비스

## 서비스 소개
여행지, 기간, 여행 스타일을 입력하면
AI가 날짜별 여행 일정을 추천해줍니다.

## 기술 스택
- HTML
- CSS
- JavaScript
- Python
- OpenAI API
- Vercel

## 주요 기능
- 여행 정보 입력
- AI 여행 일정 생성
- 추천 결과 출력
- 오류 및 빈 입력 처리
- 반응형 웹 디자인

## 실행 방법
1. GitHub 저장소 다운로드
2. 필요한 Python 패키지 설치
3. 환경 변수에 API 키 등록
4. Vercel에 배포

## 환경 변수
OPENAI_API_KEY를 설정해야 합니다.

## 배포 URL
Vercel 배포 후 URL 작성

10. 반응형 디자인
최소한 다음 화면에서 확인한다.

데스크톱
넓은 화면에 입력창과 결과창을 적절하게 배치

상단 메뉴를 가로로 표시

모바일
메뉴와 입력창을 세로로 배치

버튼을 화면 너비에 맞게 조정

AI 결과가 화면 밖으로 넘어가지 않도록 처리

11. 테스트 케이스
테스트	입력	예상 결과
정상	제주도 / 3일 / 자연	AI 여행 일정 출력
빈 입력	여행지 없음	필수 입력 안내
추가 요청	부산 / 2일 / 맛집 / 여유로운 일정	조건을 반영한 일정 출력
긴 입력	긴 여행 요청사항	결과 출력 또는 오류 안내
API 오류	AI API 오류 발생	재시도 안내

12. 최종 제출 패키지
제출할 때는 다음 5개를 준비하면 된다.

① 배포된 웹사이트
https://내프로젝트.vercel.app

② GitHub 저장소
GitHub Repository
├── index.html
├── css/
├── js/
├── api/
├── requirements.txt
└── README.md

③ 서비스 기획서
위에서 작성한 서비스 목적 / 타겟 / 페이지 / AI 기능 / 실패 처리 내용을 제출한다.

④ 스크린샷
총 3종류를 준비한다.

데스크톱 화면

모바일 화면

AI에게 여행 정보를 입력하고 결과가 나오는 화면

⑤ AI 코딩 도구 사용 증빙
AI 코딩 도구에게 코드를 작성하거나 오류를 수정하도록 요청한 대화 화면을 캡처한다.

예를 들어:

"HTML, CSS, JavaScript만 사용해서 TripMate의 반응형 여행 추천 페이지를 만들어줘."

또는

"Vercel Python API에서 오류가 발생하는데 원인을 찾아서 수정해줘."

같은 대화 기록을 캡처하면 된다.

최종적으로 만들 모습
TripMate에 접속하면

┌─────────────────────────────────┐
│ TripMate    홈  AI추천  소개     │
├─────────────────────────────────┤
│                                 │
│       ✈️ 나만의 여행 계획        │
│       AI가 여행 일정을 만들어    │
│       드립니다.                 │
│                                 │
│       [ AI 일정 만들기 ]         │
│                                 │
├─────────────────────────────────┤
│       AI 여행 추천               │
│                                 │
│ 여행지 [ 제주도       ]          │
│ 기간   [ 3일          ]          │
│ 스타일 [ 자연 + 맛집  ]          │
│                                 │
│       [ 일정 추천받기 ]          │
│                                 │
│ ───── AI 추천 결과 ─────         │
│ 1일차                             │
│ 성산일출봉 → 맛집 → 섭지코지      │
│                                 │
│ 2일차                             │
│ 한라산 → 카페 → 해변              │
└─────────────────────────────────┘

이 정도로 만들면 과제 요구사항을 빠짐없이 포함하면서도 실제 구현 범위가 과도하게 커지지 않는 프로젝트가 된다.
